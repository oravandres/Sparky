---
# Sparky Inventory
# NVIDIA DGX Spark — K3s worker node

all:
  children:
    # K3s Cluster Groups
    k3s_cluster:
      children:
        servers:
        agents:

    servers:
      hosts:
        pi-c1:
          ansible_host: pi-c1
          ansible_user: pi

    agents:
      hosts:
        sparky:
          ansible_connection: local
